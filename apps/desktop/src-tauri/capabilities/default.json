{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the desktop shell scaffold.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
