[package]
name = "k8s-ide-desktop"
version = "0.1.0"
description = "Desktop shell for the Kubernetes IDE"
edition = "2021"

[build-dependencies]
tauri-build = { version = "=2.0.0", features = [] }

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "=2.0.0", features = [] }
