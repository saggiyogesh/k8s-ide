{
  "name": "@k8s-ide/web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "dependencies": {
    "@k8s-ide/api-client": "workspace:*",
    "@k8s-ide/app": "workspace:*",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "lint": "eslint src vite.config.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --eval \"console.log('No tests for @k8s-ide/web yet')\""
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.3",
    "vite": "^8.0.16"
  }
}
